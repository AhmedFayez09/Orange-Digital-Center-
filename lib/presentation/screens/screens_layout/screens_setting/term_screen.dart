import 'package:flutter/material.dart';

class TermssScreen extends StatelessWidget {
  static const routName = 'terms';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Terms"),
      ),
    );
  }
}
