import 'package:flutter/material.dart';

class OurPartnersScreen extends StatelessWidget {
  static const routName = 'our_partner';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("OurPartner"),
      ),
    );
  }
}
